--[[ 
    Cylex-Coder: FE FLY BYPASS
    User: tubles93
    Status: FE Enabled 🛡️
--]]

local L_ScreenGui = Instance.new("ScreenGui")
L_ScreenGui.Name = "Tubles93_FE_Fly"
L_ScreenGui.Parent = game.CoreGui

local L_Main = Instance.new("Frame")
L_Main.Parent = L_ScreenGui
L_Main.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
L_Main.Position = UDim2.new(0.1, 0, 0.4, 0)
L_Main.Size = UDim2.new(0, 220, 0, 160)
L_Main.Active = true
L_Main.Draggable = true
Instance.new("UICorner", L_Main).CornerRadius = UDim.new(0, 8) -- Lekukan halus

-- HEADER
local L_Header = Instance.new("Frame")
L_Header.Parent = L_Main
L_Header.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
L_Header.Position = UDim2.new(0, 0, 0.1, 0)
L_Header.Size = UDim2.new(1, 0, 0, 32)
Instance.new("UICorner", L_Header).CornerRadius = UDim.new(0, 5)

local L_Title = Instance.new("TextLabel")
L_Title.Parent = L_Header
L_Title.Text = "FE FLY SYSTEM | tubles93"
L_Title.TextColor3 = Color3.fromRGB(255, 255, 255)
L_Title.Font = Enum.Font.SourceSansBold
L_Title.TextSize = 14
L_Title.Size = UDim2.new(1, 0, 1, 0)
L_Title.BackgroundTransparency = 1

-- TAG UNGU (T4RGET TO EVERYONE)
local L_Tag = Instance.new("Frame")
L_Tag.Parent = L_Main
L_Tag.BackgroundColor3 = Color3.fromRGB(110, 0, 210)
L_Tag.Position = UDim2.new(0, 8, 0, -10)
L_Tag.Size = UDim2.new(0, 120, 0, 24)
L_Tag.ZIndex = 5
Instance.new("UICorner", L_Tag).CornerRadius = UDim.new(0, 4)

local L_TagLabel = Instance.new("TextLabel")
L_TagLabel.Parent = L_Tag
L_TagLabel.Text = "T4rget To Everyone"
L_TagLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
L_TagLabel.Size = UDim2.new(1, 0, 1, 0)
L_TagLabel.BackgroundTransparency = 1
L_TagLabel.Font = Enum.Font.SourceSansBold
L_TagLabel.TextSize = 12

-- SPEED INPUT
local L_SpeedInput = Instance.new("TextBox")
L_SpeedInput.Parent = L_Main
L_SpeedInput.PlaceholderText = "Fly Speed..."
L_SpeedInput.Text = "50"
L_SpeedInput.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
L_SpeedInput.Position = UDim2.new(0.05, 0, 0.4, 0)
L_SpeedInput.Size = UDim2.new(0.9, 0, 0, 30)
L_SpeedInput.TextColor3 = Color3.fromRGB(255, 255, 255)
Instance.new("UICorner", L_SpeedInput).CornerRadius = UDim.new(0, 4)

-- FLY TOGGLE
local L_Toggle = Instance.new("TextButton")
L_Toggle.Parent = L_Main
L_Toggle.Text = "Fly | Off"
L_Toggle.BackgroundColor3 = Color3.fromRGB(0, 0, 210)
L_Toggle.Position = UDim2.new(0.05, 0, 0.7, 0)
L_Toggle.Size = UDim2.new(0.9, 0, 0, 35)
L_Toggle.TextColor3 = Color3.fromRGB(255, 255, 255)
L_Toggle.Font = Enum.Font.SourceSansBold
L_Toggle.TextSize = 18
Instance.new("UICorner", L_Toggle).CornerRadius = UDim.new(0, 6)

-- FLY LOGIC (FE SAFE)
local flying = false
local speed = 50
local lp = game.Players.LocalPlayer

L_Toggle.MouseButton1Click:Connect(function()
    if flying then
        flying = false
        L_Toggle.Text = "Fly | Off"
        L_Toggle.BackgroundColor3 = Color3.fromRGB(0, 0, 210)
    else
        flying = true
        L_Toggle.Text = "Fly | On"
        L_Toggle.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
        speed = tonumber(L_SpeedInput.Text) or 50
        
        task.spawn(function()
            local char = lp.Character or lp.CharacterAdded:Wait()
            local hrp = char:WaitForChild("HumanoidRootPart")
            
            local bg = Instance.new("BodyGyro", hrp)
            bg.P = 9e4
            bg.maxTorque = Vector3.new(9e9, 9e9, 9e9)
            bg.cframe = hrp.CFrame
            
            local bv = Instance.new("BodyVelocity", hrp)
            bv.velocity = Vector3.new(0, 0, 0)
            bv.maxForce = Vector3.new(9e9, 9e9, 9e9)
            
            while flying and char.Parent do
                task.wait()
                char.Humanoid.PlatformStand = true
                local cam = workspace.CurrentCamera.CFrame
                local moveDir = Vector3.new(0,0,0)
                
                local uis = game:GetService("UserInputService")
                if uis:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + cam.LookVector end
                if uis:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - cam.LookVector end
                if uis:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - cam.RightVector end
                if uis:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + cam.RightVector end
                
                bv.velocity = moveDir * speed
                bg.cframe = cam
            end
            
            bg:Destroy()
            bv:Destroy()
            if char:FindFirstChild("Humanoid") then
                char.Humanoid.PlatformStand = false
            end
        end)
    end
end)

print("Cylex-Coder: FE FLY Loaded
